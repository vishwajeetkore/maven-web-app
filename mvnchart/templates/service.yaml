apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvnchart.fullname" . }}
  labels:
    {{- include "mvnchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mvnchart.selectorLabels" . | nindent 4 }}
